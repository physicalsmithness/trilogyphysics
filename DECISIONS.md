# Trilogy Physics: DECISIONS

Newest at the bottom. Cite decisions with their substance, never bare numbers (standing principle 4). Seed set dated 2026-06-08, written by the GCSE Physics Overview seat to stand the project up. The Architecture chat owns this file from here on and should ratify, amend, or supersede these as it engages the material; several are deliberately provisional.

---

**d001 (Trilogy is its own project, a peer of Pre-IB).** Trilogy Physics is a standalone project with its own Architecture chat, not a wing of Pre-IB / single science. The two cross-fertilise as peers. Sourced from Smith, 2026-06-08.

**d002 (Architecture and Housing combined; Authoring separate; split housing only on trigger).** One chat owns both project shape and the engine/display housing, plus one or more Authoring chats. Rationale: mirrors the deliberate Fields choice, which works, and keeping schema and display in one head is the cheaper default for a single-engine project. There is a known cross-borrowing risk when housing is split out (the IB electricity display could have borrowed more from the Fields driller), but Smith is not convinced that gap was caused by the split, so this is a default-of-convenience, not a claim about cause. Trigger to split off a dedicated Housing/Implementation chat: when engine work consistently starves authoring attention, or when a shared cross-board GCSE engine emerges that needs its own owner. Sourced from Smith, 2026-06-08.

**d003 (item schema: start from the Pre-IB v0.5 structure plus the Codex calculation vocabulary; authoring may evolve it).** Use the Pre-IB topic-7 schema v0.5 (multiple qtypes, atom registries) as the structural starting point, and the Codex six-layer calculation tagging vocabulary (shape, formula/relation/rule, number sources, unit handling, mark categories, ECF gates) as the source for calculation-item attributes. This is a starting reference, not a freeze; per standing principle 5, authoring proposes schema changes from the material. Provisional pending Architecture's M1 schema lock.

**d004 (atom-level dashboard ships in v1).** The per-atom coverage and per-misconception fire/avoid display is the differentiating feature and ships in v1, not deferred to a post-telemetry milestone (standing principle 1, and the Electric Circuits M9-deferral miss). The schema logs misconception_id and applicable-but-avoided from day one.

**d005 (tier is a first-class tag; cross-tier shared items tagged consistently).** Every item carries its tier (Foundation, Higher, or both). The same question often appears as a Foundation Q05/06 and a Higher Q01; tag both consistently, mirroring the Codex data which already does this. Standing principle 6.

**d006 (cohort-results stance: difficulty signal, not coverage driver).** Use the AQA national average mark per part as a signal of what is hard and what a hard question looks like, for calibration and sequencing. Do NOT author primarily against our own cohort's weak spots: the cohort does well partly because of existing drilling, so absence-of-drilling is not the target. Sourced from Smith, 2026-06-08.

**d007 (two-pass authoring: spec-first, then past-paper-mine, then gap).** Authors first think up questions that cover the spec point (so coverage is not narrowed to what has historically been asked), then mine the full past-paper bank for how it has really been examined, then close the gap with what has not been asked but plausibly could be. Go one way, then the other. Sourced from Smith, 2026-06-08.

**d008 (every author gets the full structured bank, not just the PDFs).** Authors are given the Codex DB (full extracted, tagged bank) plus past papers plus the question-type summary, not raw PDFs alone. Sourced from Smith, 2026-06-08 ("it would be stronger if they had a full bank").

**d009 (revive the existing repo in place; fold the Magnetism bank in).** The existing `trilogyphysics` Energy (thin) and Magnetism (about 160 questions) content is real work and is folded into the locked schema rather than rewritten from scratch. Whether the existing engine is kept, refactored, or replaced is an OPEN_QUESTION for Architecture/Housing.

**d010 (provisional home: this repo on G:; revisit on sync races).** Docs and code live here, co-located, the Fields pattern. If cloud-sync racing with active multi-chat edits causes trouble (the issue that moved Electric Circuits to a non-sync drive), move to a non-sync drive and update the Cowork binding. Provisional.

**d011 (mount the GCSE Physics Overview folder read-only in every Trilogy chat).** So standing_principles.md, SPEC_MAP.md, and packets stay reachable. This is the IB side's recurring blocker; institute from the start (standing principle 3).

**d012 (home moved to the non-sync drive; supersedes d010 (provisional home on G:); resolves q-home).** The project now lives at `C:\Claude (not on Gdrive, nor OneDrive)\Trilogy Physics` (flat repo root), off Google Drive sync, exactly as Electric Circuits moved off-sync for active multi-chat editing. Done pre-emptively by Smith on 2026-06-08 (the trigger in d010 was "if sync races appear"; we took the move up front rather than waiting for a race). The `.git` history and the GitHub remote (github.com/physicalsmithness/trilogyphysics) came across intact; GitHub stays the backstop. The old `G:\My Drive\github local files\trilogyphysics` copy is now deprecated and must not be edited, to avoid divergence. Architecture chat, 2026-06-08.

**d013 (first end-to-end topic is Electricity 6.2).** Smith chose Electricity 6.2 as the first authored topic (the ROADMAP M3 slot), over the Energy/Forces candidates the seed suggested. Consequence: electricity needs circuit-diagram rendering, so it pulls in the Circuit Builder dependency from the Pre-IB side (see d014). Sourced from Smith, 2026-06-08.

**d014 (electricity-first surfaces q-shared-engine now, not at M5; engine approach UNRESOLVED pending Smith).** Reading the Pre-IB chat's work (preibphysics/new/PLAN_electricity_mode_trilogy.md, CIRCUIT_DSL.md, BRIEF_diagrams_chat.md) shows it already designed a Trilogy electricity mode as a deployment INSIDE the Pre-IB engine, and that engine already ships the assets electricity most needs: Smith's calc_workings 4-line structured-calculation type (marks-the-method, the best fit for Trilogy's calc core and principle 1), atom registries and error-type tagging (the d004 (atom dashboard in v1) machinery), and the Circuit DSL plus Circuit Builder for circuit-rules diagrams. The Circuit Builder lives in the advancedsmithics GitHub repo. That engine is materially more advanced than the trilogyphysics engine d009 (revive existing repo) assumed we would revive. So choosing electricity first forces q-shared-engine (deferred to M5 in the seed) to the front. The three live options (adopt the Pre-IB engine; keep Trilogy's own engine but port calc_workings plus the DSL into it; or build one shared GCSE engine both converge on) are with Smith. Until resolved, q-engine and the M2 retrofit are paused. Architecture chat, 2026-06-08.

**d015 (engine: Trilogy owns its engine, seeded from the Pre-IB engine; supersedes d009 (revive trilogyphysics engine); resolves q-engine; pulls q-shared-engine forward).** Smith's call, 2026-06-08. Rather than revive the weaker trilogyphysics engine or deploy inside the Pre-IB repo, we lift the strong Pre-IB engine (engine.js: the calc_workings 4-line marks-the-method calculation type, atom registries, error-type tagging via distractorErrorTypes/commonMisconceptions, the TOPIC_CONFIG multi-deployment pattern) into the Trilogy repo as Trilogy's OWN engine, and let it diverge for Trilogy's calc/explanation-heavy shape. The Circuit Builder (advancedsmithics/circuit-builder.html, a self-contained DSL-to-SVG renderer) is integrated as the `circuit` diagram kind via the TOPIC_DIAGRAMS registry. The trilogyphysics numeric grader is retired in favour of calc_workings (folded in only if it adds something). This keeps Trilogy's autonomy (d001, d002) while capturing the borrowing explicitly, which is the answer to the Fields/electricity "could have borrowed more" miss. Because electricity forced the question, q-shared-engine is opened with the GCSE Physics Overview seat NOW, not deferred to M5; the seeded-from-Pre-IB structure is kept clean enough that a later convergence stays cheap.

**d016 (engine blend: Electric Circuits Mastery drilling engine + Pre-IB calc_workings/atoms; refines d015; use ECM's Circuit Builder, not advancedsmithics).** Smith, 2026-06-08: "blend the engine from there, with its focus on repeated drilling, and the pre-IB one." Electric Circuits Mastery (`C:\Claude (not on Gdrive, nor OneDrive)\Electric Circuits Mastery`, the IB B.5 driller) is mature: `app/engine.js` (v0.14) with a mastery + event-log repeated-drilling model, an embedded Circuit Builder module (`app/circuit-builder-embed.js`, window.CircuitBuilder, newer than advancedsmithics: open circuits + arbitrary labels), a `rounding_classifier.js` (atomic numeric grading), structured data (circuit_rules, basic_circuit_formulas, misconceptions, resistor_combinations + templates, resistivity, symbols), and a two-tab sign-in modal (Quick name+cohort / Google) that IS the house login style Smith wanted. So Trilogy's engine blends ECM's drilling/mastery + Circuit Builder embed + circuit data with the Pre-IB engine's calc_workings 4-line type and atom registries. This supersedes d015's "integrate the advancedsmithics Circuit Builder": prefer ECM's embed module. ECM's B.5 question content is also a porting source for 6.2 (much transfers to AQA Trilogy circuits). Backend reporting uses ECM's reportAttempt() payload schema (wired into electricity_6_2/ now).

**d017 (the deployable is a Trilogy-wide driller, not an electricity-only app; electricity is the first populated topic).** Smith, 2026-06-08: "have it so that it's not just for electricity." The shell lives at `app/` (was `electricity_6_2/`) and presents a course-wide sign-in plus a topic picker for AQA Trilogy physics 6.1 to 6.7; Electricity 6.2 is enabled, the rest are greyed "coming soon" and flip on as each is built. One identity and one mastery profile per pupil across all topics (matches the ECM/Pre-IB TOPIC_CONFIG multi-deployment pattern and the Pre-IB course-level-abstraction directive). Reporting carries project="trilogy_physics" with the topic as a field, so one teacher sheet covers every topic.

**d018 (Housing chat split off now; Authoring chat commissioned for 6.2; supersedes the "combined" default in d002 for the build phase).** Smith prompted the delegation, 2026-06-08. Both d002 triggers are met: the ECM+Pre-IB engine blend is a sustained implementation job that would starve architecture/authoring attention, and the blended engine is a shared-lineage GCSE engine that needs its own owner. So a dedicated Housing/Implementation chat owns the engine blend (brief: inter_chat/Architecture_Housing_dispatch.md), and an Authoring chat starts Electricity 6.2 in parallel (brief: inter_chat/Architecture_Authoring_kickoff.md). This seat remains Architecture: schema lock and QUESTION_TYPES (M1), ratifying authoring proposals, and brokering q-shared-engine with the Overview seat and Pre-IB. Architecture chat, 2026-06-08.

**d019 (commission a Widgets/Diagrams chat for the non-circuit visuals; Circuit Builder keeps circuits, transformers-in-circuit, and required-practical).** Smith listed the 6.2 non-circuit widgets, 2026-06-08. A dedicated Widgets chat is justified for the same reasons as the Pre-IB diagrams commission: ~9 distinct widgets, each needing physical correctness (the filament I-V that keeps rising while flattening, AC/DC traces, NTC/LDR/wire curves), a clean self-contained interface (kind + params -> SVG via the diagram registry), parallelisable with Housing and Authoring. Catalogue and brief: inter_chat/Architecture_Widgets_dispatch.md. Reuses the Pre-IB diagrams contract (BRIEF_diagrams_chat.md). Architecture chat.

**d020 (National Grid is a 6.2 sub-topic, folded into Authoring + Widgets, not its own chat).** Smith asked whether the National Grid needs a dedicated chat. For AQA Trilogy it is modest (transformers, transmit at high voltage to cut I^2 R loss, step-up/step-down). Its questions go to the 6.2 Authoring chat; its visuals (transformer, transmission_line) to the Widgets chat. Revisit only if it grows into a cross-topic theme. Architecture chat, 2026-06-08.

**d021 (granular syllabus spine = the xlsb 'Revised syllabus v3'; 6.2 codes captured to review/SYLLABUS_6_2.md).** Smith added `aqa_physics_master_plus_patches_v8 (version 1).xlsb` with a fine hierarchical AQA Trilogy code spine (e.g. 6.2.1.4.l.iiiB) and a Higher-tier flag. The 92 Electricity 6.2 codes are extracted to review/SYLLABUS_6_2.md as the tagging reference; prose descriptions in the source are only partly completed, so wording is cross-referenced from the AQA spec at schema-lock. Items tag their finest applicable code. Architecture chat, 2026-06-08.

**d022 (item schema LOCKED v1.0; resolves q-schema-lock).** SCHEMA.md is the locked contract, baked from the ratified 6.2 vocabulary proposal, the v3 code spine (d021), the Codex six-layer calc tagging (d003), and the ECM/Pre-IB shapes (d015/d016). Every item carries board+tier (d005), subtag, finest v3 syllabus_codes, atoms, qtype, equation_sheet (from_insert vs must_recall, principle 6), per-distractor misconception_id plus applicable_misconceptions for fire/avoid (d004, principle 1), and source provenance (no invented port claims). Atom and misconception registries are data, seeded from the proposal, owned with Authoring and ratified here. Architecture chat, 2026-06-08.

**d023 (qtype set and NEW_QTYPE staging).** Gradeable now: mcq, mcq_multi, short (keyword, with the auto-mark warning), calc_workings. The three greenlit NEW_QTYPEs are staged with interim forms so authoring is never blocked: `level_of_response_6` = selectable claim-points against level descriptors until an LLM grader lands; `circuit_draw` = interim mcq with Circuit-Builder-rendered options until the Builder is an input surface (Housing); `graph_sketch` = interim mcq using the I-V widget variants (correct vs plateau vs droop, already built by Widgets) until a shape-template scorer exists. Full draw/sketch grading is a Housing/Widgets milestone. Architecture chat, 2026-06-08.

**d024 (Trilogy scope exclusions ratified from the bank reading).** Do NOT author for Trilogy: parallel-resistance reciprocal calculation (spec 6.2.2 excludes it), resistivity R=rho*L/A, potential dividers, capacitance (Triple/A-level), P=V^2/R (power is P=VI and P=I^2R only), and static electricity (Triple-only). Consequence: much of ECM's resistor_combinations/resistivity/potential_divider content does not port; the corresponding ECM misconception slugs are dropped (proposal section 3). This is the main "do not assume IB validity carries over" filter. Sourced from the Authoring chat's bank reading, ratified by Smith 2026-06-08.

**d025 (commission the 6.5 Forces widget batch; introduces interactive widgets needing an engine grading contract).** The 6.2 widgets succeeded, so Smith commissioned a large 6.5 Forces set, 2026-06-08: customisable motion graphs (d-t, s-t, displacement-t, v-t, a-t) with readings on gridlines (5 or 4 small boxes per big box, hard-ish scales); braking/stopping (velocity-time with thinking/braking/stopping distance, condition variants for speed/wet-road/bad-brakes/driver-state, plus deliberately-wrong distractor versions); distance-versus-speed stopping graph; area-under-v-t as counted squares; a gradient tool that forces a tangent on curves; free-body diagrams and ramp/incline FBDs; vector addition of forces and displacements (maths and interactive scale-diagram-plus-protractor, with distance-vs-displacement distractors); springs with clear natural-length-vs-extension; and qualitative momentum collision illustrations (sticky/bouncy/explosion). Brief: inter_chat/Architecture_Widgets_6_5_dispatch.md. New architecture point: several are INTERACTIVE (read a tangent gradient, count squares, pick a scale and read a protractor), which blurs the widget/engine boundary; interactive widgets emit a defined response and are graded by Housing's engine via a contract agreed per widget. Resources to mine: the graphingskills repo, the Fields driller (interactive gradient/area precedent), and the 6.2 widget harness. Architecture chat, 2026-06-08.

**d026 (widgets are topic-scoped chats on a shared core; Forces 6.5 gets its own chat).** Smith asked which chat owns 6.5 widgets, 2026-06-09. Decision: a dedicated Forces (6.5) Widgets chat, not the general 6.2 chat continuing and not a fresh general chat. Rationale: the 6.5 set is large and introduces interactive widgets (tangent-gradient read-off, square-count area, scale-diagram + protractor) needing a grading contract with Housing, a meatier separable concern; topic-scoping keeps each chat's context focused and scales to all seven topics; and per-topic registry files into one shared window.TOPIC_DIAGRAMS is the pattern the Pre-IB diagrams brief already anticipated. Mitigation against the Fields/electricity "could have borrowed more" divergence: reusable primitives (axis and gridline plotting, arrows, square-counting, protractor) are factored into a shared `widgets_core` module owned centrally (the general/6.2 widgets chat or Architecture), and each topic widgets chat builds on it rather than reinventing. Reversible if chat proliferation gets noisy. Architecture chat, 2026-06-09.

**d027 (commission the Forces 6.5 Authoring chat, in parallel with 6.5 widgets; 6.2 stays the end-to-end proof).** Smith, 2026-06-09. A dedicated 6.5 Authoring chat starts now: its Stage 1 vocabulary proposal is independent of the engine and widgets, so it parallelises. Brief: inter_chat/Architecture_Authoring_6_5_kickoff.md; codes in review/SYLLABUS_6_5.md. Caveat held by Architecture: 6.2 remains the topic driven all the way to a validated end-to-end slice (engine + dashboard + tracking) before 6.5 authoring goes to full volume, so pipeline lessons feed back (roadmap M3 before M4). No ECM port for Forces (ECM was circuits); auth
**d028 (Forces 6.5 vocabulary RATIFIED).** The 6.5 Forces vocabulary proposal (review/trilogy_forces_vocabulary_proposal.md) is ratified into the schema and QUESTION_TYPES. Carried: the 12 subtags including the three-way motion split (motion_descriptors / motion_graphs / acceleration), which I confirm per the diagnostic evidence (graph-gradient naming fails at 10-40% where s=vt is 90%+, so they are separately-tracked skills; OQ-B = split); ~57 atoms tier-shared (d005); the NEW_FLAG misconception set (bank-evidenced, principle 1). fbd_vector_draw greenlit as a staged NEW_QTYPE with the interim MCQ form ready now over the FBD/vector widget distractors (OQ-E, mirrors d023). proportionality_stated_as_increases promoted to the shared cross-topic/WS taxonomy (now spans 6.2 iv, springs, stopping distance; OQ-F), alongside prefix_not_converted, freehand_line_not_ruled, repeatability_reproducibility_confused, sig_figs_not_applied (q-ws-taxonomy, Overview to home). Architecture chat, 2026-06-11.

**d029 (multi-stage forces calculations = chained calc_workings stages, not a new monolithic type; resolves OQ-A).** Forces calc is multi-stage-heavy (43 non_final_evaluation marks, 35 ecf_allowed gates in the tagged slice). Rather than mint calc_workings_multistage, SCHEMA v1.1 gives calc_workings an optional ordered `stages: [ {4-line block}, ... ]` (absent or length 1 = the single-formula case already locked). Each stage is graded by the existing 4-line marker; stages link with `gate:{kind:"from_previous_part"}` so a later stage's substitution carries the previous stage's evaluated answer under ECF (ecf_allowed, matching the bank's 35-vs-4 ratio; do not manufacture hard anti-ECF gates). The atom dashboard still sees per-stage fires (principle 1: you see which stage failed). Housing implements by looping the marker with an ECF carry, no new grader logic. This unblocks the ~40% chained-calc forces bank; single-stage forces calcs were never blocked. Coordinated to Housing. Architecture chat, 2026-06-11.

**d030 (Forces 6.5 Triple-only scope exclusions ratified; cross-topic KE/GPE referenced, not duplicated; resolves OQ-C, OQ-D).** Out of Trilogy authoring (Physics-only/Triple, and zero Trilogy tag rows in the bank): terminal-velocity v-t graph DRAWING (6.5.4.1.5.j; terminal velocity stays as the qualitative 6-marker), two-body conservation-of-momentum collision CALCULATION (6.5.5.2.c.ii), and the whole changes-in-momentum / F=delta-p/t / momentum-safety subsection (6.5.5.3). Trilogy momentum (HT) is p=mv plus conservation described qualitatively. Smith can reverse any of these if a Triple build is wanted. Cross-topic KE (1/2 m v^2) and GPE (mgh), invoked by braking-energy and work-energy forces items, are REFERENCED from the Energy 6.1 atom registry via atomMap, not duplicated as forces-local atoms (OQ-D, the no-divergence principle); Architecture registers the shared KE/GPE atom ids pending 6.1 authoring. OQ-G (insert-revert difficulty cliff: seven of nine forces equations become must-recall) is noted for sequencing, carried on each item's equation_sheet flag. Architecture chat, 2026-06-11.

**d031 (widgets are central / first-class; interactive-widget grading is a core engine concern, de-staged).** Smith, 2026-06-11: "a move to make widgets more central." Many items, Waves especially, ARE the widget (mark a wavelength, drag wavefronts across a boundary, slide a prism into a beam), so interactive widgets are a primary question-and-grading surface, not decoration. Consequences: (1) interactive-widget grading moves from "later milestone" (d023/d025) to a CORE Housing concern, contracted per widget between the topic Widgets chat and Housing; (2) the schema gains a first-class interactive-widget question pathway, the widget emits the pupil's response (placed mark, read angle, dragged position) in a defined shape and the engine grades it against an author-specified target (SCHEMA to add a `widget_response` qtype/contract at v1.2); (3) Widgets chats are a permanent central part of the topology, one per topic on the shared widgets_core (d026), not a side service. Build order for any interactive widget: static render first, interactive layer second with the Housing contract. Architecture chat.

**d032 (commission the Waves 6.6 Widgets chat).** A dedicated topic-scoped Waves Widgets chat on the shared widgets_core (d026 pattern), per Smith's 6.6 catalogue (2026-06-11). Brief: inter_chat/Architecture_Widgets_6_6_dispatch.md; codes review/SYLLABUS_6_6.md (64 codes). Scope: transverse wave-train anatomy (markable wavelength/half-wavelength/amplitude, peak-trough arrows, double/diagonal-amplitude distractors), plan-view wavefronts, longitudinal compressions/rarefactions, wave scenarios (shore ripples, pier counting, echolocation, speed-of-sound methods), ripple tank and standing-wave (RP) measurement set-ups, the EM spectrum (author-markable regions/frequency/wavelength ends) and lightly its origins/uses, refraction of wavefronts and rays through triangular/rectangular/semicircular prisms (interactive drag), material wavelength-dependent behaviour, and the infrared radiation RP (Leslie cube, wax/bulb, black-vs-shiny bottles with cooling curves, IR detection). Heavy interactivity, so the d031 static-then-interactive build order and Housing grading contracts apply. The chat reads the 6.6 spec and Smith's notes for the measurement-method variations. Architecture chat, 2026-06-11.

**d033 (commission the Magnetism 6.7 Widgets chat).** Topic-scoped on shared widgets_core (d026), per Smith's 6.7 catalogue (2026-06-11). Brief: inter_chat/Architecture_Widgets_6_7_dispatch.md; codes review/SYLLABUS_6_7.md (43 codes). Scope: bar-magnet and two-magnet fields (with rich wrong-field distractors: touching/crossing lines, lines through the magnet, lopsided, not meeting the magnet, reversed arrows), electromagnet/solenoid, induced magnetism and poles, magnetic materials, compass and Earth's field, iron-filings/compass field mapping, straight-wire end-on field (right-hand grip, distance/spacing distractors), and the HT motor-effect set (Fleming's left-hand rule with a drawn hand, motor-effect rigs on pivots/scales, the d.c. motor at several rotation angles). 3D (into/out-of-page) required throughout. Heavily interactive (d031). It salvages and improves the existing 7_Magnetism SVGs rather than starting cold. Architecture chat.

**d034 (6.7 has existing salvageable content; harvest and retrofit, do not revive the old engine; reduce self-marking; updates d009 and task #5).** Smith, 2026-06-11: the previous Magnetism work (the ~160-question 7_Magnetism bank) has usable question ideas but rough illustrations, weak keyword marking, and padded "ten at a time" near-duplicate series. Approach when the 6.7 Authoring chat is commissioned: mine 7_Magnetism/questions.js for the good items, dedupe the padded series, retag to the locked schema (SCHEMA v1.x), replace the illustrations with the new 6.7 widgets, and grade through the blended engine (d015), NOT the old engine. Honour the "overhaul self-marking" steer: minimise the keyword-marked `short` qtype; prefer mcq / mcq_multi / calc_workings / the staged widget_response / level_of_response claim-points (the motor-effect F=BIL is the one HT calculation, calc_workings). This supersedes d009's "revive the existing engine" framing (the engine is already decided, d015) and refines task #5 from "dedupe/retrofit" to "salvage + retag + re-illustrate + re-grade". Architecture chat, 2026-06-11.

**d035 (interactive widget = first-class `widget` qtype; ratifies the TOPIC_WIDGETS contract; unifies errorCodes into the misconception registry; resolves the SCHEMA v1.2 half of d031).** The Widgets 6.5 chat proposed and built a clean interactive-grading contract (window.TOPIC_WIDGETS factory -> {getAnswer, score, destroy}, pure-model scoring, per-kind errorCodes), mirroring the Fields driller. Architecture ratifies it as SCHEMA v1.2: items use `qtype:"widget"` with `widget:{kind,config}` parallel to `diagram:{kind,params}`. One ruling added: widget errorCodes ARE misconception slugs (registered or mapped), so the d004 dashboard has ONE taxonomy across mcq distractors, calc_workings, and widgets, not three. Housing to wire window.TOPIC_WIDGETS as a second lookup path and the qtype:"widget" submit flow, as it did for TOPIC_DIAGRAMS. widgets_core ownership: the general/6.2 Widgets chat owns it (ratifying the core-split thread); topic chats consume; Architecture arbitrates primitive changes; the 6.2 migration to consume the core is low-priority (no divergence risk while the duplicated primitives stay untouched). Architecture chat, 2026-06-11.

**d036 (MCQ options may render a diagram/widget, not just text; ratifies Forces batch 1 and its 3 NEW_FLAGs).** The 6.5 forces_basics batch (12 items, app/topics/forces_6_5.js) is ratified: schema-compliant, faithful to the AQA bank, every distractor misconception resolves to a registered slug, validation passed. Its three NEW_FLAGs are accepted into the misconception registry: scalar_vector_definition_confused, contact_noncontact_misclassified (distinct from its specific sub-case air_resistance_called_noncontact), force_not_an_interaction. The batch surfaced a real engine gap: renderMcq currently renders option TEXT only, so the interim "which diagram is correct?" forms of the staged types cannot be authored. Ruling (SCHEMA v1.3): an MCQ option may be {text} | {diagram:{kind,params}} | {widget:{kind,config}}, rendered via the registry, with per-option misconception preserved. Housing to extend renderMcq. This unblocks the interim pick-the-correct-diagram/graph/FBD/circuit forms across 6.2/6.5/6.6/6.7. Architecture chat, 2026-06-11.

**d037 (the multi-stage calc catalogue is canonical shared authoring reference; validates d029).** Smith's Google Sheet "List of Possible (Four-to-)Six-Marker Calc Questions" is captured at review/MULTISTAGE_CALC_CATALOGUE.md, the canonical list of chained 4-to-6-mark calculations across all seven topics, with Smith's per-item likelihood (a coverage/priority signal) and harder-variant notes. Every author authors these as chained calc_workings with stages (d029); it validates that design and the cross-topic KE/GPE referencing (d030/OQ-D). Authoring uses likelihood to prioritise within comprehensive two-pass coverage (d007), not as the sole driver (cf d006). Wired into the 6.2 and 6.5 kickoffs; the default reference for 6.1/6.3/6.4/6.6/6.7 when commissioned. Architecture chat, 2026-06-11.

*Note (Housing, 2026-06-09): the two decisions below were renumbered from d022/d023 to d038/d039 after a concurrent-append collision with Architecture's d022 (schema lock) and d023 (qtype staging). Same content, new numbers.*

**d038 (engine blend M2 spine built behind the `app/` shell; lifts ECM verbatim, calc_workings to-spec pending the Pre-IB source).** Housing chat, 2026-06-08. The blended engine is standing and runs end to end behind the existing course-wide shell: `app/engine.js` (TOPIC_CONFIG-driven, one engine for all seven topics per d017 (Trilogy-wide driller)), `app/diagrams.js` (`window.TOPIC_DIAGRAMS` registry, the hook Widgets needs per d019 (Widgets chat commissioned)), `app/topics/electricity_6_2.js` (config with `items:[]` for Authoring, plus `?demo=1` fixtures). The seam keeps the shell authoritative for identity + reporting (`TrilogyEngine.mount({container,topic,config,identity,report})`); the engine emits per-attempt events to the shell's report callback, payload matching the ECM `reportAttempt()` schema. LIFTED VERBATIM FROM ECM (d016 (engine blend: use ECM's Circuit Builder embed)): `circuit-builder-embed.js`, `formula.js`, `rounding_classifier.js`, the event-log mastery model, the MCQ grader, `slugs_offered` fire-vs-avoid (the d004 (atom dashboard in v1) "track non-fires"), greenness/ticks, and the per-atom mosaic colouring. The d004 atom dashboard ships from day one (per-atom mastery mosaic + per-misconception fired-vs-avoided). BLOCKER recorded for Smith/Architecture: the Pre-IB engine file carrying `calc_workings` (`preibphysics/new/engine.js`) is an online-only Google-Drive path, empty in the session mount, so it could not be lifted verbatim; calc_workings is built to the documented contract as an interim, isolated to one function (`gradeCalcWorkings`) for a cheap later swap. Verified with 44 automated assertions (29 headless graders/registry + 15 jsdom full-mount). Supersedes nothing; advances ROADMAP M2. The `short` qtype is implemented as claim-point selection pending q-explanation-grader; calc method-marking is a self-check reveal pending the Pre-IB lift.

**d039 (calc_workings lifted verbatim from the Pre-IB engine; source reached via the GitHub Pages mirror, not the Drive mount; completes the d038 interim).** Housing chat, 2026-06-09. The real four-line marks-the-method grader (Pre-IB engine §3b-bis/§3b-ter, v1.5.22 to v1.5.26: the recursive-descent equation parser, `markCalcWorkings`, the ERROR_TYPES taxonomy and `calcDeriveErrorTypes`) is now lifted verbatim into `app/calc_workings.js` (`window.TrilogyCalcWorkings`), replacing d038's value-only interim. It marks each of the four lines (equation, substitution, rearrangement, final value+unit) independently so method earns credit, accepts algebraically-equivalent substitutions, does case-sensitive unit checking, and emits canonical line-level error codes (equation_wrong, sub_inconsistent, algebra_error, unit_missing, value_wrong, etc.) onto the attempt event. ACCESS NOTE for the estate: the `G:\My Drive\...` (Google Drive) folders never surface their bytes into a Cowork session, even when set "available offline" and freshly re-mounted (they enumerate directories but show empty/placeholder files); the reliable routes are (a) the public GitHub Pages mirror `https://physicalsmithness.github.io/preibphysics/` via raw fetch, or (b) copying a file into a C:/OneDrive folder, which surface fine. This is worth recording because the same Drive-mount limitation will recur for any chat told to lift from a Drive-hosted repo. Verified: 59 automated assertions green (36 headless incl. the calc grader, 23 jsdom incl. the four-line UI rendering and marking 4/4 with per-line results).

**d040 (engine reconciled to the locked SCHEMA v1.0; adopts short=markPoints and tier F|H|FH).** Housing chat, 2026-06-09. The blended engine (d038) and its demo items + tests are aligned to SCHEMA.md v1.0 and the ratified 6.2 vocabulary: qtypes `mcq`/`mcq_multi` graded by `choices[]` + `answerIndex(es)` with per-distractor `misconception_id` (and the d036 diagram/widget option forms); `short` switched to free-text `markPoints:[{any:[synonyms]}]` keyword-presence marking with the auto-mark warning shown (this supersedes the d038 interim's claim-point selection, honouring the d022 (schema lock) / d023 (qtype set) ruling); `tier` is F|H|FH with the dashboard filter all/F/H; events carry `atoms[]` + `subtag` and the dashboard groups the per-atom mosaic by subtag; calc_workings `knowns:{SYM:{value,...}}` is flattened to the lifted grader's input; fire/avoid reads `applicable_misconceptions` plus per-choice slugs. The 6.2 TOPIC_CONFIG now carries the ratified 10 subtags, 37 atoms, and the misconception label subset (canonical registry still owed in data/misconceptions, owned with Authoring). Verified: 70 automated assertions green (40 headless + 30 jsdom, every qtype rendered and graded through the DOM). One item for Architecture: I adopted the schema's keyword `short` as locked; if true free-text/LLM short grading is wanted later that remains an open grader decision.

**d038 (recognise the intermediate "prep-step" 3-marker calc tier).** Smith, 2026-06-11: between single-step plug-and-chug and the catalogued multi-stage chains (d037) sits a common 3-mark tier where one non-trivial step obtains a quantity before a single formula, a graph read-off, a difference (delta-theta, delta-v, leftover p.d.), a resultant sum, or a conversion. Authors cover these deliberately (they are not in the catalogue). They are calc_workings authored either as a short leading prep `stage` feeding the formula (d029), or as a single stage whose known carries a non-trivial Codex number-source tag (graph_read / difference / resultant / prefix_conv) so the prep step is gradable and diagnosable separately from the formula. Recorded in QUESTION_TYPES. Architecture chat, 2026-06-11.

**d039 (commission the Waves 6.6 and Magnetism 6.7 Authoring chats).** Smith, 2026-06-11. Both start now (Stage-1 vocab is engine/widget-independent). 6.6 brief: inter_chat/Architecture_Authoring_6_6_kickoff.md (widget-central topic, d031; 6.6 widgets already delivered). 6.7 brief: inter_chat/Architecture_Authoring_6_7_kickoff.md (salvage the existing 7_Magnetism bank per d034; 6.7 widgets in build). Both reference the multi-stage catalogue (d037) and the calc tiers (d038). All five content topics (6.2, 6.5, 6.6, 6.7 now have authors; 6.1/6.3/6.4 remain) plus four widget chats and Housing. Architecture chat, 2026-06-11.

**d041 (canonical misconception registry shipped at data/misconceptions.js; engine reads it; completes the d040 owed item).** Housing chat, 2026-06-09. `data/misconceptions.js` (`window.TRILOGY_MISCONCEPTIONS`, 45 slugs) is built from the ratified vocabulary proposal §3 in the ECM slug/topic/label/description schema (d016), `topic` = 6.2 subtag (or `cross`/`ws`). It carries the ported-valid, port-trimmed, and NEW_FLAG slugs; the retired `wrong_power_form_for_topology` and the out-of-scope reciprocal/resistivity/potential-divider slugs are deliberately absent (proposal §3, d024 (scope exclusions)). The engine reads labels from this registry at mount (canonical), with per-topic config overlaying. Authors set a distractor `misconception_id` to one of these slugs. Owned with Authoring; Architecture ratifies. Verified: 75 assertions green (45 headless + 30 jsdom).

**d042 (interactive `widget` qtype wired into the engine; second registry `window.TOPIC_WIDGETS`; errorCodes unified into the d004 dashboard).** Housing chat, 2026-06-09. Implements the ratified SCHEMA v1.2 widget pathway (d031 interactive grading is core; d035 ratification) requested by the 6.5/6.6/6.7 Widgets threads. The engine adds `qtype:"widget"` with `widget:{kind,config}`: it mounts `window.TOPIC_WIDGETS[kind](host, config)`, on Submit calls `instance.getAnswer()` then `instance.score(answer, config)`, maps the returned `status` (full/partial/none) to correct/half/wrong, and writes `marksAwarded/marksPossible/status/error_codes` onto the attempt event. errorCodes are treated as misconception slugs (d035): `computeMisconceptionTable` now counts `misconception_id` + calc `error_types` + widget `error_codes` as fires across one taxonomy, so the d004 fire/avoid dashboard unifies MCQ, calc_workings, and widget errors. `widgets_core.js` + the topic widget files (forces/waves/magnetism-diagrams.js) are wired into `app/index.html` (core first); they register 15 interactive kinds, 46 static diagram kinds, and the pure `*_MODELS` scorers (which also allow headless re-scoring). Verified: 85 assertions green (45 headless + 40 jsdom, incl. real widget-file composition and the full mount→getAnswer→score→log flow). Slug registration: per Architecture's ruling the widget errorCode slugs are registered in the canonical misconception registry by Architecture/topic owners; the engine resolves labels from `window.TRILOGY_MISCONCEPTIONS` (+ per-topic config) and falls back to the raw slug until then.

**d040 (ratify the 6.5 and 6.2 authoring batches; approve 6.2 calibration to scale+wire; SCHEMA v1.4).** Forces 6.5 batches 1-6 (forces_basics, weight_gravity, springs_elasticity, motion_descriptors, motion_graphs, acceleration; 67 items, 6/12 subtags) RATIFIED: schema-compliant, validation-run, two-pass, faithful, interim prefix/power MCQs correctly tagged (interim_for) pending the Housing grader fixes, the multi-stage demonstrator (acc_lightgates_chain) per-stage-correct pending the ECF loop. Electricity 6.2 calibration batches (resistance_ohm + iv_characteristics, 22 items) RATIFIED: the item shape is approved, so the 6.2 author SCALES and WIRES both into app/topics/electricity_6_2.js, replacing the provisional atom registry with the ratified slugs (config + items updated together). This is the unblock for the empty 6.2 bank (the author was awaiting calibration approval, not idle). NEW_FLAGs registered: elastic_inelastic_confused (springs), sensor_direction_reversed and filament_resistance_falls (iv), plus the batch-1 forces three (already d036). SCHEMA v1.4 adds the calc.codex tagging block, the item-level subtag field, and the calc ERROR_TYPES->slug crosswalk. The "accept 3 mA for 0.003 A" finding folds into the Housing prefix-grader fix. No-diagram recall variants of the sensor-direction items blessed (recall vs read-off design tension). Architecture chat, 2026-06-11.

**d043 (Waves 6.6 vocabulary RATIFIED).** Exceptionally well-evidenced proposal (review/trilogy_waves_vocabulary_proposal.md) ratified into the schema and QUESTION_TYPES. Carried: 10 subtags incl. the wave_basics/wave_properties split and the three-way em_origins/em_dangers/em_uses split (OQ-D, confirmed on the diagnostic evidence, mirrors d028); ~50 atoms tier-shared; the large bank-evidenced NEW_FLAG set; the widget errorCodes registered as misconception slugs per d035 (the mapping table in the proposal stands). Rulings: OQ-A echo/travelling-wave chains reference the 6.5 speed_calc atom via atomMap with echo_distance_doubling as the waves-local d=2x atom (confirmed, d030 pattern); OQ-B author echo items only in Trilogy-safe frames (clap-off-wall, EM radar; sonar/ultrasound allowed as bare context, no ultrasound-specific physics); OQ-C 6.6.1.2.h is IN as qualitative v=fλ reasoning (bank-evidenced x4) avoiding the formal sound-between-media wording; OQ-E do NOT author total-internal-reflection (not Trilogy); tir_not_recognised stays registered-but-dormant; OQ-F accept the two new WS slugs (uncertainty_given_as_range, digital_reading_trusted_no_random_error) and promote graph_readoff_left_as_answer to the shared WS taxonomy (q-ws-taxonomy, Overview-owned); OQ-G insert cliff (v=fλ recall, T=1/f given) noted for calibration. 6.6 is the first topic authorable almost entirely in v1.3 (widget-central, no NEW_QTYPE). Architecture chat, 2026-06-11.

**d044 (Magnetism 6.7 vocabulary RATIFIED; salvage inventory accepted).** Excellent bank-deep proposal (review/trilogy_magnetism_vocabulary_proposal.md) ratified. Carried: 7 subtags (OQ-1: split magnets_and_fields three ways into magnets_and_poles / induced_magnetism / magnetic_fields, confirmed on the diagnostic evidence - induced fails 1-25% while pole attraction sits at 90%+, the d028 logic); ~35 atoms tier-shared; the bank-evidenced NEW_FLAG set; the Scope exclusions (induction/generators/transformers/loudspeakers are Physics-only, out of Trilogy; the National Grid transformer story stays homed in 6.2 per d020). Rulings: OQ-2 FLHR orientation family authored as diagram-option MCQs now, upgrading to the interactive flemings_lhr widget (d035) when its Housing contract lands, authored once with both renderings in mind; OQ-3 F=BIl is on BOTH inserts (select-and-apply), so equation_sheet:from_insert permanently, exempt from the insert-reversion cliff; OQ-4 W=mg/V=IR/P=IV and N3 referenced via atomMap (d030) to the owning 6.2 (ohm_law_calc, power_vi_calc) and 6.5 (weight_calc, n3_equal_opposite) atoms, not 6.7 duplicates; OQ-5 default ecf_allowed for Trilogy 6.7 chains (the Trilogy bank shows only ecf_allowed); do NOT manufacture the hard anti-ECF gate on Trilogy items (it is Synergy-specific, principle 6 board fidelity), though it is a known harder shape; OQ-6 magnetic_material_treated_as_magnet homes in induced_magnetism and cross-fires on electromagnet items via applicable_misconceptions. The salvage inventory (~162 old items -> ~60 ideas, dedupe the padded series, retag, re-illustrate with the new widgets, re-grade, near-zero keyword short) is accepted and IS the d034 plan; it also confirms the old twoMagnets SVGs were unreachable (Widgets finding). Author starting magnets_and_poles + induced_magnetism. Architecture chat, 2026-06-11.

**d045 (level_of_response_6 interim grader built; completes the gradeable-qtype set).** Housing chat, 2026-06-09. Implements the d023 staged interim for the AQA 6-mark level-of-response item: selectable claim-points against level descriptors (the LLM grader remains a later milestone). An item carries `lor:{ points:[{text,creditworthy,misconception_id?}], bands?:[{minHits,marks,label}] }` and `marks:6`; the pupil ticks the points they would include, and the grader scores by net creditworthy points (a wrong pick costs a mark), mapped to the 6-mark scale (author `bands` override the default mapping) with a Level 1/2/3 band label. Wrong points carrying a `misconception_id` are logged as `error_codes` into the unified d004 fire/avoid table. With this, the engine grades the full SCHEMA qtype set: mcq, mcq_multi (incl. per-option diagram/widget, d036), short (markPoints), calc_workings (verbatim Pre-IB), widget (d042), and level_of_response_6; the `circuit_draw`/`graph_sketch` interims ride the diagram/widget option path. Verified: 97 assertions green (49 headless + 48 jsdom).

**d046 (Magnetism bank folded to SCHEMA v1.0 via a migration; completes the M2 retrofit mechanically, judgment flagged for Authoring).** Housing chat, 2026-06-09. `tools/migrate_magnetism.js` lifts the 162-item legacy bank (`7_Magnetism/questions.js`) into a SCHEMA v1.0 TOPIC_CONFIG at `app/topics/magnetism_6_7.generated.js`. The legacy bank was already index-based MCQ and `{any:[synonyms]}` markPoints, so mechanical mapping is clean: type->qtype (mcq; short; the 21 `numeric` F=BIl items -> `calc_workings` value-only, upgradeable to four-line), choices->[{text}], answerIndex passthrough, difficulty easy/med/hard -> d1/d2/d3, explanation/marks. All 162 items are servable by the engine. JUDGMENT is flagged per item under `_authoring.needs` for the 6.7 Authoring chat: tier (defaulted FH), finest syllabus_codes, per-distractor misconception_id, and confirmation of the PROVISIONAL subtag/atom registries (derived from legacy tags, pending a ratified 6.7 vocabulary proposal). 12 near-duplicate groups flagged for culling. Report: `review/magnetism_migration_report.md`. The topic stays greyed in the picker (`enabled:false`) until Architecture ratifies the 6.7 vocab and flips it on; the config loads now so it is testable. Verified: 56 headless assertions incl. servability + sample grading of migrated items. Owns no content decisions: the retag is Authoring's.

**d047 (ratify the new batch wave; SCHEMA v1.5: marked-calc structure, graph_sketch, the fallback pattern).** Ratified: 6.2 series_parallel (16 items, the ECM circuit-rules port in action) and power_electrical+energy_appliances; 6.5 multistage_calc_chains, newtons_laws, terminal_velocity (now 8/12 subtags, ~93 items); 6.6 wave_basics + wave_properties (batch 1, 24 items). New misconception slugs registered: chain_prep_stage_skipped, chain_intermediate_as_final (multi-stage failure modes), medium_travels_with_wave (waves), appliance_energy_confused_power_energy (power); new atom identify_topology (surfaced from the ECM topology items, principle 5). SCHEMA v1.5 formalises: (a) the Smith-directed marked/multi-stage calc structure (calc.stages, per-stage markScheme with Codex Layer-5 categories, markCategories, failsAt on distractors, facility_pct/difficulty/source) so the hard catalogue chains carry their full marking data even as MCQ-interim; (b) graph_sketch as a NEW_QTYPE graded by shape predicates against the Widgets target curve (the model is the answer key); (c) the generic fallback->self_check pattern for any ungraded qtype. Rulings on findings: power_energy retro-fix the marks:4 prefix claim to an interim_for twin (consistent with the pending prefix-grader fix); series_resistance_sum can become calc_workings once Housing confirms the grader parses a two-term sum (route); the two-wave same-scale comparison is a non-blocking Widgets enhancement (wave_train compare:true). The Housing-generated 6.7 migration (magnetism_6_7.generated.js, 162 items) is acknowledged; the 6.7 author curates it per the salvage plan (d044). Architecture chat, 2026-06-11.

**d048 (Forces 6.5 and Waves 6.6 authoring COMPLETE and ratified; circuit_draw qtype; 6.7 widget slugs registered).** Ratified the final batches: 6.5 resultant_forces, work_done, stopping_distance, momentum -> **Forces 6.5 COMPLETE (12/12 subtags, 126 items, full 60-atom coverage)**; 6.6 wave_calculations, wave_measurement, refraction_boundaries, em_spectrum, em_origins, em_dangers, em_uses, infrared_radiation -> **Waves 6.6 COMPLETE (10/10 subtags, 109 items, full 61-atom coverage)**. 6.2 circuit_basics ratified; `circuit_draw` greenlit as a NEW_QTYPE on the d047 fallback pattern (Builder-as-input grader, SCHEMA v1.6, routed to Housing); pick-the-symbol covered by d036 diagram-option MCQ. SCHEMA v1.6 also promotes chain_prep_stage_skipped/chain_intermediate_as_final to the shared cross-calc set and adds the notation/standard-form output check to the grader wishlist. Magnetism 6.7 interactive widgets (flhr_direction, mark_poles) delivered and Housing-wired (d042); their six generic errorCodes (direction_reversed, gave_field_direction, gave_current_direction, direction_wrong_axis, poles_reversed, pole_wrong) registered as misconception slugs per d035. Rulings: errorCodeMap default-for-induced = YES (bake poles_reversed -> induced_magnet_expected_to_repel into scoreMarkPoles for induced stimuli; authors may override); the compass-needle drag is deferred (the d036 diagram-option MCQ over compass/field_mapping covers the 8-direction plotting staples; build the drag only if the 6.7 author asks for gap-pass items); keep the standing-wave loop=lambda error under wavelength_half_marked (same half-cycle-as-full trap family, no split). Architecture chat, 2026-06-11.

**d049 (generic fallback self-check built; first of the three d047 grader asks; unblocks graph_sketch and every staged qtype now).** Housing chat, 2026-06-09. Per the d047 ask and the graph_sketch_sample contract (§B), the engine now serves ANY item whose qtype it does not natively grade but which carries a `fallback` block: it renders the prompt (+ axes note), a "Reveal model answer" control that draws `fallback.reveal` through the existing TOPIC_DIAGRAMS renderer, the `self_mark_prompt`, and three self-mark buttons, logging the attempt with `grading: fallback.log_as` (default `ungraded_self_assessed`). Such attempts are excluded from the per-atom accuracy (greenness + mosaic) so self-marks do not pollute mastery stats, while still recording exposure. `isServable` now passes an unknown qtype that carries `fallback`. This lets graph_sketch (and any future NEW_QTYPE) ship as a self-check the day it is authored. Verified: 113 assertions green (60 headless + 53 jsdom incl. the full reveal -> self-mark -> ungraded-log flow). The other two d047 builds (chained calc_workings with ECF; graph_sketch shape grader) are scoped in the Housing thread.

**d050 (chained multi-stage calc_workings grader built with ECF carry; second of the three d047 asks).** Housing chat, 2026-06-09. When a calc item carries `calc.stages` (each a single-block spec: equation/knowns/unknown/expectedFinalValue/expectedUnit + markScheme), the engine renders a stage-by-stage four-line UI and grades it by looping the lifted single-block marker (`markCalcWorkings`) per stage. ECF (`gate:{kind:"from_previous_part"}`): the stage known equal to the previous stage's expected value is replaced with the PUPIL's own previous-stage final value (value-match), and for an isolated-unknown equation the stage's expected value is recomputed from the carried known, so a wrong earlier value still earns later method marks. Per-stage marks scale the lifted marker's /4 to the stage's markScheme length; the total scales to item.marks; status full/partial/none -> correct/half/wrong; the reveal shows per-stage line ticks and which stage carried forward. v1 limitation flagged to Architecture: a NON-isolated stage equation (e.g. v^2-u^2=2as) keeps the official expected value rather than recomputing from the carried value, so its substitution/eval lines only score under ECF when the carried value matches; the equation (method) line still scores. The interim mcq_single chain items grade as MCQ today; flipping their qtype to calc_workings activates the chain grader. Verified: 121 assertions green (63 headless incl. full/partial/ECF chain cases + 58 jsdom incl. the two-stage UI rendering and marking 4/4). Third d047 ask (graph_sketch shape grader) remains gated on a sketch-input widget from Widgets; the d049 fallback self-check covers it meanwhile.

**d051 (commission the Particle Model 6.3 and Atomic Structure 6.4 Authoring chats).** Smith, 2026-06-11. Briefs: inter_chat/Architecture_Authoring_6_3_kickoff.md and Architecture_Authoring_6_4_kickoff.md; code spines review/SYLLABUS_6_3.md (40) and SYLLABUS_6_4.md (64). 6.3 is calculation-rich (rho=m/V, dE=mc d-theta, E=mL, plus the catalogue heater chains as enriched marked calc_workings d047, cross-topic power/weight via atomMap). 6.4 is the cross-fertilisation topic: its primary source is the Pre-IB topic-7 Radioactivity bank (the most advanced authored bank in the GCSE estate, ~200 q, six atom registries), PORTED AND RETAGGED to AQA Trilogy with board fidelity (principle 6), recall/explanation-heavy with half-life and nuclear-equation work. Both flag widget needs (heating/cooling curves and particle diagrams for 6.3; atom/decay/half-life diagrams for 6.4) for a later widget commission; interim via diagram-option MCQ (d036) and the fallback self-check (d047). With these, 6.2-6.7 all have authors; only Energy 6.1 remains unstaffed. Architecture chat, 2026-06-11.

**d052 (Architecture wired Forces and Waves into the shell).** With Housing's punch-list item A unactioned and Smith asking for a live slice, Architecture applied the two-line wiring directly to app/index.html (2026-06-11): script tags for topics/forces_6_5.js and topics/waves_6_6.js after electricity, and enabled:true for 6.5 and 6.6. Both topic files parse clean; engine supports their qtypes (tests green). So once the repo is committed+pushed, Forces (126) and Waves (109) are drillable, served at .../trilogyphysics/app/ if Pages serves the repo (the old drillers already are). Housing: pull this before editing index.html to avoid a conflict; the rest of the punch-list (calc-grader fixes, new graders, magnetism enable) is still yours. Architecture chat.

**d053 (item-shape conformance: migrate Waves and Magnetism to the engine shape; Forces is the only live, working finished topic).** A pre-wiring audit found waves_6_6.js and magnetism_6_7.js authored in the early item shape (mcq_single / distractors / status / tier:"both"), which the engine does NOT read - they render blank. The locked engine shape (confirmed against engine.js field reads) is JSON-keyed: "qtype" in {mcq, mcq_multi, short, calc_workings, level_of_response_6}, "choices":[{..., "misconception_id"}] + "answerIndex"/"answerIndices", "tier" in {F,H,FH}, "atoms":[], "syllabus_codes":[], "applicable_misconceptions":[]. Forces was migrated to this by its author (the reusable Node transform in review/forces_6_5_migration_and_enhancement.md is the template) and is engine-shaped and live (d052). Electricity's demo fixtures are engine-shaped but its real items are not pasted yet. Action: migrate waves_6_6.js and magnetism_6_7.js with that transform (Housing or the respective authors); both stay enabled:false until migrated (the Waves enable from d052 is reverted today). magnetism_6_7.js (the authored fork) supersedes magnetism_6_7.generated.js - wire the fork. STANDING RULE: author to the engine JSON shape, and conformance-check a topic file before flipping it enabled. Architecture chat, 2026-06-11.

**d054 (Waves + Magnetism item-shape migration done; authored magnetism fork wired; renderLor lede generalised; actions the d052/d053 punch-list).** Housing chat, 2026-06-16. Read Architecture's d052 (Forces/Waves wired by them; pull before editing index.html) and d053 (waves_6_6.js + magnetism_6_7.js still in the stale shape, render blank; migrate reusing the Forces transform; wire the authored magnetism_6_7.js not the .generated.js; keep 6.6/6.7 disabled). Done: `tools/migrate_item_shape.js` migrates a topic file from the old shape (mcq_single / distractors[{status}] / claims / tier "both") to SCHEMA (mcq + choices[]+answerIndex(es), claim-short -> level_of_response_6.lor, tier F|H|FH, atoms[]/syllabus_codes[]), preserving every answer key. Ran on waves_6_6.js (109 items: 86 mcq, 5 mcq_multi, 10 lor, 5 calc_workings, 3 widget) and magnetism_6_7.js (29 items: 22 mcq, 3 mcq_multi, 4 lor); originals backed up to outputs/*.premigration.bak.js. Cross-checked 116 MCQ answer keys against the backups: 0 mismatches; all 138 items servable by the engine. index.html now script-includes `topics/magnetism_6_7.js` (the authored fork supersedes the generated file per its header). Both topics LEFT enabled:false: migration (the stated blocker) is cleared and both render, but flipping the picker flag is Architecture's readiness call (6.7 still wants author curation). Also fixed the Forces author's flag: `renderLor` lede is now generic ("This is a N-mark answer" from item.marks) instead of hardcoded 6-mark. Did NOT touch Architecture's index.html enable flags or the Forces wiring. Tests 121 green.

**d055 (REGRESSION: level_of_response_6 and mcq_multi broke in the latest engine.js; integration suite red; fix before trusting the live slice).** After Housing's recent engine.js work (shape-migration era), test/integration.js regressed from 0 failures to 6: level_of_response_6 fully broken (options-rendered, marked+banded, graded, creditworthy-shown all FAIL) and mcq_multi partially (circuit-stimulus render + correct-option marking FAIL; core grading still passes). All six were green at the prior run. Both qtypes are used by the ENABLED topics (Electricity has 1 mcq_multi + 5 level_of_response_6; Forces/Waves carry LOR claim-points), so Electricity and Forces are currently partly broken in the engine. Smoke tests (calc_workings + registry) still pass (63). Housing to bisect against the latest engine.js change and restore green before we trust or extend the live slice; do NOT enable Waves/Magnetism until the suite is green again. Architecture chat, 2026-06-17.

**d056 (calc_workings evaluator fixes: ^ power + multi-letter/case-distinct symbols; 2 of 3 d047 priority fixes; + calc.codex event passthrough).** Housing chat, 2026-06-17. Fixed two of the three calc grader limits the Forces author hit. (a) **`^` power operator**: restructured the recursive-descent parser so `^` is right-associative and binds tighter than implicit multiplication (k e^2 = k*(e^2), not (k*e)^2), added the token and Math.pow eval; unblocks v^2-u^2=2as, Ek=1/2mv^2, Ee=1/2ke^2. (b) **Multi-letter / case-distinct symbols**: when a question's symbol set has any multi-letter symbol or a case collision, the marker remaps each symbol to a distinct single-letter placeholder (reserving e and x), marks on the remapped strings via the single-letter core, and maps the displayed working back; so "Ee" is one symbol and E (energy) != e (extension). Single-letter items keep the legacy path, so all prior tests stay green. Verified on real content: all 47 Forces calc equations now parse (0 failures); EPE/KE/SUVAT squared items grade full. Also added the SCHEMA v1.4 loader item: `calc.codex` six-layer block passes through to the attempt event (item-level `subtag` was already read; the calc ERROR_TYPES already count in the unified d004 table). STILL OUTSTANDING (d047 fix 3): prefix/unit-conversion grading (award a prefix_conv mark from a known's {value,unit,asGiven}, and accept a prefixed-equivalent final answer) - a distinct mark-category feature, scoped for next. The cross-stage ECF loop is done (chained grader, d050). Verified: 127 assertions green (69 headless + 58 jsdom).

**d057 (prefixed-equivalent final answers accepted; completes the d047 calc evaluator fixes as far as the authored data supports).** Housing chat, 2026-06-17. The line-4 grader now accepts a prefixed-equivalent final answer (3 mA == 0.003 A, 450 mA == 0.45 A, 710 g == 0.71 kg, 12 kΩ == 12000 Ω) via an SI-prefix table and base-unit match (mass kg/g handled specially), without over-accepting (450 A != 0.45 A; a wrong base unit like mV for an A answer is still rejected). NOTE on fix 1 (the asGiven prefix_conv mark): no authored item uses the schema's {value, unit, dimension, asGiven} known shape — every calc known is a flat SI number (the "prefix" item ohm_vir_prefix_02 has knowns {V:2.7,I:0.45}), so there is no asGiven data to award a separate prefix_conv line-mark from, and a failed in-prompt conversion already loses the substitution line. The asGiven mark is moot until authors adopt that shape; one function wires it in if they do. With this the three d047 evaluator fixes are addressed: ^ power (d056), multi-letter/case-distinct (d056), prefix handling at the answer line (here); the cross-stage ECF loop was done at d050. Verified: 133 assertions green (75 headless + 58 jsdom).

**d058 (d055 "regression" was a test-fixture collision, not an engine bug; demo fixtures extracted from the authored config to test-only).** Housing chat, 2026-06-17. Architecture's d055 priority report (integration suite 0->6 failures on LOR + mcq_multi) was NOT an engine regression. Root cause: my `?demo=1` demo items lived inside `app/topics/electricity_6_2.js`, and the dedicated integration tests selected items by qtype; when the 6.2 author populated the bank, those filters started catching real authored items (a 4-point LOR where the test expected the 8-point demo), so the assertions failed. Proven not an engine bug: all 31 real level_of_response_6 items (5 in 6.2, 16 in 6.5, 10 in 6.6) grade `full` on their creditworthy sets, and all 13 real mcq_multi items (1/7/5) grade `correct` on their answer keys and reject a wrong pick. Structural fix so it cannot recur and the authored bank stays clean: the 8 demo items were extracted from electricity_6_2.js into `test/fixtures_6_2.js` (`window.TRILOGY_TEST_FIXTURES`, loaded by the integration test only); the shipped config is now pure authored content (83 items, all servable, zero `_demo`), and the integration suite builds its config from the fixtures, fully decoupled from authored items. Release gate (integration suite) green: 58/58, deterministic over repeated runs; smoke 75/75. Supersedes the demo-in-config pattern from the early scaffolding.

**d059 (Waves 6.6 enabled - third live topic; d055 closed as not-a-bug per d058).** With the engine gate green (smoke 75, integration 58) and the calc-grader fixes landed (d056/d057), I verified waves_6_6.js via its verifier (109/109 items servable and grading full/correct, zero stale markers, valid qtypes mcq/mcq_multi/calc_workings/level_of_response_6/widget) and flipped 6.6 enabled:true (conformance-check-before-enable, d053). Live topics now: Electricity 6.2 (83 authored items), Forces 6.5 (126), Waves 6.6 (109) = ~318 questions. The d055 "regression" is closed: Housing showed (d058) it was a test-fixture collision, not an engine bug, and extracted the demo items to test/fixtures_6_2.js. Magnetism 6.7 stays disabled until its author finishes curating; 6.3/6.4 not yet started; 6.1 unstaffed. Architecture chat, 2026-06-19.

**d060 (playtest rulings, Smith 2026-06-19; full log review/PLAYTEST_FEEDBACK_2026-06-19.md).** From a live 6.2 drill. STANDING RULES: (A1) the "use the equation X" stem framing must follow the formula_catalogue insert flags - only present an equation as given when it is on the 2023 insert; recall equations stay equation_sheet:"must_recall" even if printed in-stem; audit all items (a wind-turbine E=Pt item presented a recall equation as given). (A2) never attach an answer-giving or contradictory stimulus diagram (LDR R-vs-light shown on a recall item; AC trace on a DC question) - correct kind/params per item, no-giveaway variant on recall items. ENGINE (Housing): (H1) calc_workings feedback must name the specific error (wrong-quantity substitution, un-converted prefix) not just "two sides don't agree" - and this is the real case for adopting the asGiven prefix shape so prefix errors are caught (revisits d057). (H4, Architecture design ruling) separate "rearrange" from "evaluate" where a genuine rearrangement exists (5th line / per-item configurable) and tighten line-1/line-2 acceptance so a wrong-way formula or wrong-quantity substitution does not pass; the current single line over-accepts. (H2) shuffled-deck no-repeat-until-exhausted selection (items repeated within ~15 Q). (H3) surface and filter by tier (F/H). (H5) make the atom-dashboard per-attempt colour update legible. WIDGETS: (W1) graph axis labels belong OUTSIDE the plot, y-label opposite side, x-label clear of the curve (collisions on the AC/DC and LDR graphs) - fix in widgets_core, affects all topics. (W2) circuit layout variety (switches/batteries on side walls, rotated) via the DSL wall modifiers. Provenance: questions are real (Codex bank), not generated; the defect is tagging, not fabrication. Architecture chat, 2026-06-19.

**d061 (AMENDS d060 H4: calc_workings uses a FIXED line structure always, not a conditional 5th line; ping both marks when a combined step is ambiguously wrong).** Smith, 2026-06-19: a conditional rearrange line signals to the pupil whether a rearrangement is needed, which leaks information. So the line structure is the SAME on every calc_workings item regardless of whether the equation needs rearranging (the rearrange line is always present; the pupil echoes/repeats it when no rearrangement is required, allowRepeat covers this) - its presence carries no information. Marking: when rearrange and evaluate are combined and a wrong answer cannot be localised to either the rearrangement or the evaluation (e.g. a rearrangement done for the wrong calculation), the grader docks BOTH associated marks ("ping both") rather than guessing which step failed. The separate over-acceptance fix still stands: tighten line-1 (reject a wrong-way-round equation) and line-2 (reject wrong-quantity substitution). Architecture chat, 2026-06-19.

**d062 (Architecture+Housing, 2026-06-20): Atom dashboard v2 built from the EdTech Patterns Menu.** Smith was unhappy with how atoms show, how colour shows, how they are grouped, and whether you can filter. I read the EdTech Overview "Patterns subproject" catalogue (`EdTech Overview\patterns\PATTERNS_MENU.md`, code-verified) and implemented the picked bundle directly in `app/engine.js` + `app/engine.css`, respecting the existing topic>subtag>atom grouping (atoms carry `.subtag`; `M.config.subtags` supplies labels). Changes:
- **Colour [P-COL-BAND6]:** replaced the coarse 4-band mosaic (green .8 / amber .5 / red) with the canonical estate six-band scale (untried parchment, red <.25, orange .25-.50, yellow .50-.70, midgreen .70-.90, green >=.90), verbatim hexes, each band carrying a legible `text` foreground. White-fade for low evidence kept and capped at 0.7 [P-COL-WHITEFADE]. A six-swatch legend + plain-language note now sit at the foot [P-SLI-COVERAGEMAP].
- **Grouping [P-LVL-TREE + P-LVL-ATOM]:** subtag groups are now collapsible (caret, state persisted in PREFS.collapsed) with a roll-up swatch on the header coloured by the mean of its tried atoms ("NN% across k/total atoms tried"). Colour still lives only at atom + roll-up level. Orphan atoms (subtag not in the declared list) fall into an "Other" group.
- **Filtering [P-MIS-MISTAKEROUTING + P-EXC-ACTIVEFILTER]:** clicking an atom cell, a group roll-up swatch, or a misconception row sets a focus filter (atom / subtag / slug) that narrows the served pool via `isServable`; a removable filter-chip strip + "Clear all" + a live "N questions match" note prevent a persisted filter from stranding the pupil. Empty-pool message updated to point at the chips.
- **State:** PREFS gains `focus:{atom,subtag,slug}` and `collapsed:{}`, persisted; `setFocus`/`clearFocus` reserve a fresh question from the narrowed pool and repaint. New pure helper `atomAverage(topic,slug)` [P-COV-LASTN].
Verified: `node test/smoke.js` 75/0, `node test/integration.js` 58/0 (existing dashboard assertion still green), plus an interaction harness confirming 6 cells/2 groups/2 swatches/6-band legend render, atom-click narrows the pool to the matching item and raises one chip, Clear all clears, and collapse hides a group's atoms. Backups left at `app/engine.js.bak.d062` and `app/engine.css.bak.d062`. This is a first cut for Smith to look at; colour cuts, default collapse state and whether focus should persist across sessions are all easy to tune. Not yet committed/pushed (Windows-only).

**d063 (Architecture+Housing, 2026-06-22): standalone units driller folded into the sign-in and the tracking backend.** Smith asked for `physics_skill_driller_units_v4_1.html` (the prefixes/units skill driller, already hosted at `.../trilogyphysics/physics_skill_driller_units_v4_1.html`) to be reachable from the signed-in app and to record progress. Done without touching the driller's own local stats:
- **Shared identity (same origin).** The driller and the app shell are both on `physicalsmithness.github.io`, so they share `localStorage`. Added a small reporter IIFE to the driller that reads the shell's identity key `trilogy_physics_identity_v1` and reuses the `trilogy_session` session id.
- **Reporting.** `window.TrilogyUnitsReport(partial)` POSTs to the same Apps Script `/exec` as the shell, in the identical payload schema (project `trilogy_physics`, identity fields, session, item_id/qtype/status, topic tag `units_driller_v4_1`), no-cors + keepalive. Hooked into `updateStats(score)` (the single grading choke-point): every submitted attempt reports `item_id = mode + ":" + targetId`, `qtype = mode` (prefixes|units), `status` correct/wrong, and the typed answer (capped 60 chars). Only posts when signed in.
- **Banner.** On load the driller shows a thin bar: green "Saving your progress, NAME (CLASS)" when signed in, or amber "Not signed in, so your progress will not be saved. Sign in." linking back to `app/index.html` otherwise. Practice still works either way.
- **Link from the shell.** Added a "Maths & units practice" card under the topic grid in `app/index.html` linking to `../physics_skill_driller_units_v4_1.html`.
Verified: all four inline scripts `node --check` clean; a jsdom harness confirms the reporter reads the seeded identity, POSTs the correct schema to the script.google.com endpoint with the right item_id/status/topic, shows the signed-in banner, and (no identity) posts nothing and shows the sign-in banner. Backups at `physics_skill_driller_units_v4_1.html.bak.d063` and `app/index.html.bak.d063`. Not yet committed/pushed (Windows-only); rows land once pushed and a signed-in pupil uses it. Note: this is honour-system identity shared via same-origin localStorage, not authenticated; the same caveat as the shell login.

**d064 (Architecture+Housing, 2026-07-06): the two legacy topic drillers (1_Energy, 7_Magnetism) folded into sign-in + tracking, same pattern as d063.** Both standalone drillers (`1_Energy/index.html` + `engine.js`, `7_Magnetism/index.html` + `engine.js`, hosted at `.../trilogyphysics/1_Energy/` and `/7_Magnetism/`) now carry the shared-origin reporter shim: reads the shell identity (`trilogy_physics_identity_v1`), reuses the `trilogy_session` id, and POSTs each graded attempt to the same Apps Script `/exec` in the shell schema. `window.TrilogyDrillerReport` is defined in each index.html with a per-folder topic tag (`energy_driller` / `magnetism_driller`). Hooked at each engine's single record choke-point: Magnetism in `checkAndShow` (after `updateOverallForInstance`), Energy in `saveOutcome` (after `saveProgress`); both map the engines' full/partial/wrong to correct/half/wrong and send item_id (question base id), qtype where available, and score/max in the `mode` slot. Same green/amber sign-in banner (its link is `../app/index.html` from the subfolder). The shell picker's "More practice (standalone drillers)" section now lists all three: Prefixes & units, Energy (older), Magnetism (older). Verified: both engines `node --check` clean, hook present once each, jsdom confirms the reporter posts the correct schema with the right topic tag + identity and shows the banner. Backups at `*/index.html.bak.d064`, `*/engine.js.bak.d064`. Not committed/pushed yet (Windows-only). Note: the older drillers use their own local progress store as before; this is additive backend reporting only, and 7_Magnetism overlaps the not-yet-authored 6.7 topic, so the sheet will carry both `magnetism_driller` (legacy) and eventual `6.7_magnetism` (new engine) rows.
