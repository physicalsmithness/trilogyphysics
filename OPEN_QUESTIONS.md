# Trilogy Physics: OPEN_QUESTIONS

Each has a triggering condition for when it must be resolved. Owned by Architecture-and-Housing.

- **q-engine: RESOLVED 2026-06-08 (d015 (Trilogy owns its engine, seeded from Pre-IB)).** Neither revive the trilogyphysics engine nor deploy inside Pre-IB: Trilogy gets its OWN engine seeded from the strong Pre-IB engine (calc_workings, atom/error-type schema, TOPIC_CONFIG), with the Circuit Builder integrated as the `circuit` diagram kind. The trilogyphysics numeric grader is retired in favour of calc_workings.
- **q-explanation-grader (resolve before authoring an explanation-heavy topic at M3):** how does the grader handle 2-to-6-mark written explanation questions? Free-text matching, selectable claim-points, or phased sub-questions? Trilogy's explanation load makes this load-bearing; Pre-IB's lighter explanation work does not answer it.
- **q-schema-lock (resolve at M1):** ratify, amend, or replace the seed schema (DECISIONS d003) after reading the Codex data and the Magnetism bank.
- **q-shared-engine (PULLED FORWARD from M5 by d015 (engine seeded from Pre-IB); now an active cross-project thread, not yet resolved):** electricity-first meant Trilogy's engine is seeded from Pre-IB's, so the two now share a lineage. Whether they later converge on one engine/display is opened with the Overview seat now, kept cheap by keeping the seeded structure clean. Not decided; flagged early instead of deferred.
- **q-codex-mount (resolve when authoring starts):** the parent `C:\CodexProjects\PaperDatabases\` is needed for `outputs/previews/`, but only the `Trilogy Categorisation` subfolder is currently mounted. Ask Smith to add the parent for authoring chats.
- **q-home: RESOLVED 2026-06-08 (d012 (home moved to the non-sync drive)).** Moved to `C:\Claude (not on Gdrive, nor OneDrive)\Trilogy Physics`, off Drive sync, with `.git` and the GitHub remote intact. The old G: copy is deprecated.
- **q-separate-physics (resolve if Smith a