# Trilogy Physics: OPEN_QUESTIONS

Each has a triggering condition for when it must be resolved. Owned by Architecture-and-Housing.

- **q-engine (resolve at M2):** keep, refactor, or replace the existing `trilogyphysics` engine? The Magnetism build works but predates the locked schema and the dashboard requirement. Decide before retrofitting the Magnetism bank.
- **q-explanation-grader (resolve before authoring an explanation-heavy topic at M3):** how does the grader handle 2-to-6-mark written explanation questions? Free-text matching, selectable claim-points, or phased sub-questions? Trilogy's explanation load makes this load-bearing; Pre-IB's lighter explanation work does not answer it.
- **q-schema-lock (resolve at M1):** ratify, amend, or replace the seed schema (DECISIONS d003) after reading the Codex data and the Magnetism bank.
- **q-shared-engine (resolve at M5, not before):** do Trilogy and Pre-IB converge on one engine/display, brokered by the Overview seat? Premature to decide while both are mid-flight.
- **q-codex-mount (resolve when authoring starts):** the parent `C:\CodexProjects\PaperDatabases\` is needed for `outputs/previews/`, but only the `Trilogy Categorisation` subfolder is currently mounted. Ask Smith to add the parent for authoring chats.
- **q-home (resolve if sync races appear):** stay on G: beside the code, or move to a non-sync drive as Electric Circuits did (DECISIONS d010)?
- **q-separate-physics (resolve if Smith asks):** when and how AQA Separate/Triple Physics (8463) joins as an extension of Trilogy.
