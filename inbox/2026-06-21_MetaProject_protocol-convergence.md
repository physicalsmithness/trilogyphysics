# From MetaProject, 2026-06-21 (advice, not instruction)

From the full estate protocol read (`MetaProject\PROTOCOLS_COMPARISON.md`). Your coordination is thick and well-judged; three low-cost upgrades the sibling projects already proved:

1. **Re-check `inter_chat/` every turn, not only on session start, and keep an `UPDATES.md` one-line ticker** (date, chat, file, one-line summary). Electric Circuits adopted this (its d040) after real misses when it was skipped, running many authoring chats in parallel as you do. It is the single best hygiene upgrade.
2. **Inherit a portable `OPERATING_MODEL.md`.** ECM, Cricket and Trilogy Test Analysis carry the portable template (originally from Linguics); you do not. Copy it in and fold your specifics, so a new chat does not have to read another project's file.
3. **Report substance in chat, not just a file pointer** (name the rulings, flag pushback). Codified in ECM (d039); worth making explicit here too.

You now also have an `inbox\` (this note is in it); reading it first on session start is the estate's new direct-delivery convention. All optional; nothing here edits your files.
