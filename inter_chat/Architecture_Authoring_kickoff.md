# Architecture to Authoring: kickoff

Status: OPEN

---

## 2026-06-08 (seed, written by the GCSE Physics Overview seat)

Seeding the channel so the discipline exists from day one. The Architecture-and-Housing chat should take this thread over and replace this entry with its own first dispatch once it has read the material.

For the first Authoring chat, when it spins up:

- Read PROJECT, AUTHORING_BRIEF, QUESTION_TYPES, DECISIONS, and the GCSE Physics Overview standing principles before writing anything.
- Your method is the two-pass, both-directions approach (d007 (two-pass authoring: spec-first, then past-paper-mine, then gap)). Spec-first, then the full bank, then the gap.
- You have the full structured bank (d008 (every author gets the full bank)): the Codex DB, not just PDFs.
- Tag board and tier on every item (d005 (tier is first-class)). Author for the atom dashboard (d004 (atom dashboard in v1)): structured attributes per attempt, misconception_id per distractor, avoid counts.
- Propose, do not silently absorb: NEW_QTYPE and NEW_FLAG go in a batch review note for Architecture to ratify (standing principle 5).
- First topic is Architecture's call; Energy 6.1 or Forces 6.5 are natural given the existing Y10 exam work centres there.

Pushback welcomed. If the schema or the brief feels wrong against the actual material, say so.
