# Batch review: em_spectrum (Waves 6.6, batch 5)

From: 6.6 Waves Authoring. Date: 2026-06-11. Items in `app/topics/waves_6_6.js` (ids `es_*`), 12 items.

**Covered:** 6.6.2.1.a-e. Order (fill-the-gap over an `em_spectrum` render with `blanks`, and the 2018 UV-position item with its named between-microwave-and-visible error); both ends (gamma greatest frequency at 55%, radio longest wavelength from the 2019 box-fill where a third scored zero); the inverse f-lambda pairing (the 2018 UV two-statement `mcq_multi` at 15%, list principle preserved by exact-set marking); common properties (the 2024 "one OTHER property" trap where repeating the stem scores nothing, and the energy-transfer F item at 70%); continuity; eyes-detect-only-visible; colour order (violet, 27%, red the named error); and place-a-wavelength (the 2021 2H 6.5x10^-7 m item, sub-30%, where the failure is nm/mm/standard-form comparison, tagged prefix_not_converted on the infrared distractor).

**Misconceptions exercised:** em_order_confused, spectrum_ends_swapped, freq_wavelength_inverse_missed, red_called_shortest, property_answered_with_use, ultrasound_treated_as_em, prefix_not_converted, medium_travels_with_wave. The widget `em_spectrum` region-select item (which_end / name_region configs) is deliberately NOT duplicated here as a qtype:"widget" item since the MCQ forms above cover the same atoms; one can be added in a sweep once Housing wires the widget flow, zero new vocabulary needed.

**Awkward:** nothing. The two `mcq_multi` items rely on exact-set marking (the engine's existing grader), which models AQA's list principle adequately.
