# Batch review: refraction_boundaries (Waves 6.6, batch 4)

From: 6.6 Waves Authoring. Date: 2026-06-11. Items in `app/topics/waves_6_6.js` (ids `rb_*`), 11 items.

**Covered:** 6.6.2.2.b (name the process, 2023 F at 20%), 6.6.2.2.c (ray construction, FH: the normal's role at 40%, into-glass and out-of-glass directions, the parallel-emergent-ray block result, plus ONE true `qtype:"widget"` drag-the-refracted-ray item on the d035 contract), 6.6.2.2.b/d HT (the speed cause with the 2025 ignored-density ruling baked into distractors; the 3-mark wavefront explanation as claim-points over the `refraction_wavefronts` render; frequency-constant-at-a-boundary), and 6.6.2.2.a HT (violet-refracts-most, the 2019 grade-8/9 item at 1%, and the glass-blocks-UV material-behaviour shape). TIR deliberately absent (d043 OQ-E); `tir_not_recognised` stays dormant.

**Forms:** the interim for true ray DRAWING (d023 pattern) is spot-the-property MCQs plus the widget drag; once Housing wires d036 diagram-options, the "which ray diagram is correct?" picture form can be added over the named refraction renders without new authoring vocabulary.

**Misconceptions exercised:** refraction_term_unknown, bent_wrong_way, equal_angle_no_refraction, normal_not_drawn, density_cited_not_speed, speed_change_direction_unstated, boundary_frequency_not_constant, red_called_shortest, plus snell_angle_off reachable via the widget scorer.

**Widget param note (for the record):** the built `refraction_ray` static takes `shape:"rectangle"|"triangle"|"semicircle"`, `n`, `theta1`, `mark:{normal,angles,labels}`; the interactive config takes `n1/n2/theta1`. Items use these exact names (two early drafts said "rectangular"/"incidence_deg"; corrected before commit).

**Awkward:** the 2019 prism item (complete the ray through a triangular prism, 60% zero) needs the true drawing surface to be faithful; its content is covered by rb_ray_into_glass + rb_block_emergent + the widget, but the prism-specific deviation-toward-the-base drawing is queued for the d036 picture-MCQ form (`shape:"triangle"` renders exist). Nothing else.
