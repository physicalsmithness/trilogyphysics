# Batch review: terminal_velocity (Forces 6.5, batch 9)

From: 6.5 Forces Authoring chat. Date: 2026-06-11. Output appended to `app/topics/forces_6_5.js`.

## What it covers
- **Spec code:** 6.5.4.1.5.i (qualitative). The v-t graph *drawing* for terminal velocity (6.5.4.1.5.j) is Physics-only and excluded (proposal scope).
- **Atoms (all 3):** `terminal_velocity_concept`, `air_resistance_speed` (x2), `terminal_velocity_explain` (x3).
- **Items: 6.** 4 `mcq_single`, **2 `short`** (the explanations). No calc - terminal velocity is qualitative in Trilogy.
- **Misconceptions exercised:** `terminal_velocity_named_average`, `terminal_velocity_balance_missed`, `terminal_velocity_explained_as_tiring`, `accel_equated_with_force`. No NEW_FLAGs.

## Highlights
- `tv_explain_short` is the iconic 6-marker (authored as `level_of_response` interim claim-points): weight > air resistance -> accelerate; air resistance rises with speed; air resistance = weight -> resultant zero -> constant velocity. The two distractor claims are the exact examiner-flagged failures: "stops because they get tired" (`terminal_velocity_explained_as_tiring`, "85% scored zero... most just explained why it slows") and "acceleration becomes equal to the air resistance" (`accel_equated_with_force`, the dimensional category error "describing acceleration as being equal to air resistance").
- `tv_parachute` covers the new-lower-terminal-velocity reasoning (area up -> drag up -> drag > weight -> decelerate -> new balance).

## Coverage so far
8 of 12 subtags = **93 items**. 60 atoms, 56 misconceptions. Remaining: resultant_forces, work_done, stopping_distance, momentum.
