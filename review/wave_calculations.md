# Batch review: wave_calculations (Waves 6.6, batch 2)

From: 6.6 Waves Authoring. Date: 2026-06-11. Items in `app/topics/waves_6_6.js` (ids `wc_*`), 17 items.

**Covered:** 6.6.1.2.d/f (+ 6.6.1.2.g.ii.i for the echo chain). Three layers per the d038 tiers:

- **Tier 1, single-formula `calc_workings` (5)**: v=fλ all three ways (1650-Hz speed at 93% facility; the 2024 string rearrangement; the 2019 long-number frequency at 52%/12% H/F), T=1/f and f=1/T (the 2020 ECF-pair shape). All grade full on the current grader; λ authored as the single symbol `L` per the 6.5 multi-letter constraint. `equation_sheet` carried: v=fλ from_insert now but the recall variant exists as the must_recall MCQ (22% vs 90%+, the OQ-G cliff).
- **Tier 2, prep-step (2, enriched)**: graph-read → v=fλ (the 2022 sound-temperature shape, with `graph_readoff_left_as_answer` and `stem_numbers_multiplied` distractors) and the 2021 2H means-then-convert-then-v=fλ (4mk, ~5% full marks; `prefix_not_converted` cm→m).
- **Tier 3, the d037 chains (4, enriched-MCQ per the 6.5 batch-7 pattern)**: travelling waves v=fλ→v=d/t (L5, 5mk); the full triple f=1/T→v=fλ→v=d/t (L4, 6mk); echo clap-off-a-wall d=2x (L5, 4mk, Trilogy-safe frame per d043 OQ-B); radar EM echo (L5 frame, 4mk, standard form). Each carries `calc.stages` (d029 blocks, gated from_previous_part), per-mark `markScheme` with Codex Layer-5 categories, and per-distractor `failsAt`. These chains are gap-pass coverage: the tagged bank has only 2 multi-stage parts.

Plus the prefix interims (3, `interim_for:"calc_prefix"` per the 6.5 grader limit): kHz (2022, 10% full), GHz (2025 F, "1/48 = 48"), and the nm-on-output item (2025 H, <10%) — the proposal's headline that 6.6 is the prefix/standard-form heartland. And the two qualitative v=fλ items (ripple-depth 2021 2H <20%; speed-temperature-time 2023 2F 25%) per the d043 OQ-C ruling.

**Misconceptions exercised:** wave_equation_inverted, reciprocal_not_taken, prefix_not_converted (x3 forms incl. nano-on-output), power_of_ten_evaluation_error, stem_numbers_multiplied, graph_readoff_left_as_answer (WS, d043), faster_means_longer_time, boundary_frequency_not_constant, echo_factor_two_missed, mean_across_different_quantities, chain_prep_stage_skipped, chain_intermediate_as_final (both added to the registry from the 6.5 set), wrong_formula_rearrangement.

**Registry note:** `chain_prep_stage_skipped` and `chain_intermediate_as_final` (6.5 NEW_FLAGs, awaiting ratification there) are now also used by 6.6 — supporting their promotion to the shared cross-calc set when Architecture ratifies the 6.5 batch flags.

**Awkward:** the standard-form-OUTPUT gate (2024 2H "give your answer in standard form") is not expressible to the current calc grader (it checks numeric value, not notation); the nm/standard-form items carry it in MCQ form instead. Worth a line in the grader wishlist alongside the prefix extension — same fix territory (notation checks on the line-4 value). Nothing else.
