# Batch review: wave_measurement (Waves 6.6, batch 3)

From: 6.6 Waves Authoring. Date: 2026-06-11. Items in `app/topics/waves_6_6.js` (ids `wm_*`), 13 items.

**Covered:** 6.6.1.2.g.ii (speed of sound, both spec methods), g.iii / .i.iA (ripple tank), .i.iB (waves on a string, the standing-wave half RP20), .i.ii (analysis: means, precision, uncertainty, error type). The topic's catastrophic method 4/6-markers are authored as claim-point `short` items (interim level_of_response per d023, honouring the d034 anti-keyword steer): ripple tank (10% L2/L3 on the real papers), string RP (10%), direct speed of sound, and the 2018 echo method (9%, with the throw-the-bricks distractor preserved for the record). Each claim set encodes the two recurring failure modes as wrong claims: `apparatus_described_not_method` and `measurement_purpose_missing`.

**Skill traps as MCQs**, several over live diagram stems (`ripple_tank`, `standing_wave loops:3/4`, `wave_scenario` variants speed_clap/echo_wall/pier): measure-across-several-wavelengths (why + the 2023 scale-factor computation), nodes = loops + 1 (`counted_loops_not_nodes`, the widget code, spec-first since AQA has not yet asked it), loop = half-wavelength, pier frequency count (with the 60/12 period inversion), mean of repeats (the one high-facility item, 70%), precision vs accuracy (40%), uncertainty = half range (2025, 18%), and random error on a digital probe (2025, 15%, the examiner-named misconception).

**Misconceptions exercised:** apparatus_described_not_method, measurement_purpose_missing, single_wavelength_measured, echo_factor_two_missed (claim form), counted_loops_not_nodes, wavelength_half_marked (standing-wave analogue), period_frequency_confused, stem_numbers_multiplied, mean_across_different_quantities, graph_scale_misread, repeatability_reproducibility_confused, uncertainty_given_as_range (WS, d043), digital_reading_trusted_no_random_error (WS, d043).

**Awkward:** (1) The standing-wave wavelength item tags the loop=lambda error with `wavelength_half_marked` — semantically the same trap family (half-cycle taken as a full cycle); if Architecture prefers a standing-wave-specific slug (`loop_taken_as_wavelength`), say so and I will split it. (2) The 2025 uncertainty/error items sit on RP21 codes (6.6.2.2.e.i) though they are generic WS skills; codes follow where AQA actually asked them. Nothing else.
